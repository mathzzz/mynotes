# schema 
  在软件工程中，schema 泛指系统组件之间交换数据的格式约定或模型。它不仅仅是数据库结构，还包括 API 请求/响应格式、配置文件结构、事件消息格式等
  + 前后端协作：定义明确的数据契约。 API 契约：OpenAPI（Swagger）规范定义的 JSON schema，描述端点、参数、响应体结构
  + 验证与安全：防止非法数据进入系统。XML Schema (XSD)：定义 XML 文档的合法结构
  + 版本演化：通过 schema 兼容性规则（向后/向前兼容）实现平滑升级。JSON Schema：用于验证 JSON 数据的格式

  在数据库中，描述数据库的结构定义（表、列、约束、索引等）
  Schema 都是关于数据的元数据（metada）描述——它描述了数据的结构、类型、关系，而不是数据本身

# 皮尔逊相关系数
  两个变量的协方差除以它们标准差的乘积。
  皮尔逊相关系数 (r)：进行故障定位/容量预测。观察“并发连接数”与“CPU 占用率”的相关性。如果 $r$ 接近 $1$，说明当前的 CPU 压力主要是连接数激增导致的；如果 $r$ 很低，说明 CPU 忙于其他任务（如内核态的中断处理或特殊的 DPDK 逻辑）。
# 变异系数
  单个变量相对于其平均水平的“波动”程度。
  变异系数 (CV)：衡量抖动 (Jitter)。如果一段链路的平均延迟是 20ms，标准差是 2ms，CV 就是 $10\%$。如果另一路延迟 200ms，标准差 20ms，CV 也是 $10\%$。在网络稳定性评价中，CV 比单纯看标准差更能反映链路质量的相对一致性。
